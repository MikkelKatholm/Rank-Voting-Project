# To be written


# Note to self
To run the progeam run
```bash
# 🧭 Navigate to
cd Coding/mp-spdz-0.4.1/My_scripts

# Make executable
chmod -x run_scripts.sh

# Run the election
./run_script.sh -c <num_candidates> -s <num_servers> -v <num_votes> -f <field_size_in_bits>"
```
